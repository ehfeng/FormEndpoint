{
	"scripts": {
		"dokku": {
			"postdeploy": "flask db upgrade; VERSION=$GIT_REV; sentry-cli releases new -p formendpoint $VERSION; sentry-cli releases set-commits $VERSION --commit ehfeng/FormEndpoint@$VERSION"
		}
	}
}